<h1>All spaces</h1>
<ul>
<% @spaces.each do |space| %>
  <%= link_to space_path(space) do %>
    <li>Name: <%= space.name %></li>
    <li><%= cl_image_tag space.photo, height: 300, width: 400, crop: :fill %></li>
  <% end %>
<% end %>
</ul>

<br>
<%= link_to "Add Space", new_space_path %>

